name: dart_ipfs_client
description: A client library for the IPFS HTTP API.
version: 0.0.1
homepage: https://github.com/hackyguru/IPFS-Flutter

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  chopper: ^7.0.0
  json_annotation: ^4.9.0
  logging: ^1.3.0

dev_dependencies:
  build_runner: ^2.4.0
  chopper_generator: ^7.0.0
  json_serializable: ^6.8.0
  lints: ^4.0.0
  test: ^1.25.0
